# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# Environnement
.env

# Données collectées (archives PDF/HTML) — l'actif vit en prod + sauvegardes, pas dans git
# Ancré sur le dossier d'archives ; le référentiel bundlé backend/app/data/ reste versionné.
/backend/data/

# Fichiers de travail des consolidations assistées (regénérables). Celui des
# rapprochements attributaire ↔ annuaire porte des données nominatives non
# vérifiées : il ne doit jamais partir dans un dépôt public.
fusion_propositions.csv
attributaires_propositions.csv
attributaires_personnes.csv
projets_propositions.csv

# Divers
*.log
.DS_Store
.playwright-mcp/
node_modules/
*.zip

# Captures Playwright à la racine (docs/captures reste suivi)
/*.png

# Documents de travail internes (cadrage, veille) — ne pas publier
/docs/cadrage-plateforme-civique-v2.md
/docs/rapport-sources-donnees-temps-reel.md
